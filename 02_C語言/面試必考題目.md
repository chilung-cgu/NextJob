# 📋 C 語言面試必考題目

> 這份文件包含 IC 韌體面試最常見的 C 語言題目  
> 建議：每天練習 3-5 題，持續 4 週

---

## 🔴 第一類：指標（Pointer）— 必考！

### 基礎概念

**Q1：什麼是指標？**
```c
指標是一個變數，儲存另一個變數的「記憶體位址」。

int a = 10;      // a 是一個整數變數，值為 10
int *p = &a;     // p 是指標，儲存 a 的位址

// & 取址運算子：取得變數的位址
// * 取值運算子：取得位址指向的值
```

**Q2：以下程式碼的輸出是什麼？**
```c
int a = 5;
int *p = &a;
int **pp = &p;

printf("%d\n", *p);      // 輸出？
printf("%d\n", **pp);    // 輸出？
```
<details>
<summary>點擊查看答案</summary>

```
5
5

解釋：
- *p 取得 p 指向的值，即 a 的值 = 5
- **pp 先取得 pp 指向的值（即 p），再取得 p 指向的值（即 a）= 5
```
</details>

---

**Q3：指標算術**
```c
int arr[] = {10, 20, 30, 40, 50};
int *p = arr;

printf("%d\n", *p);        // 輸出？
printf("%d\n", *(p+2));    // 輸出？
printf("%d\n", p[3]);      // 輸出？
p++;
printf("%d\n", *p);        // 輸出？
```
<details>
<summary>點擊查看答案</summary>

```
10
30
40
20

解釋：
- *p 取得 arr[0] = 10
- *(p+2) 取得 arr[2] = 30（指標加 2 表示向後移動 2 個 int 的大小）
- p[3] 等價於 *(p+3) = arr[3] = 40
- p++ 後，p 指向 arr[1]，所以 *p = 20
```
</details>

---

**Q4：sizeof 與指標**
```c
int arr[5] = {1, 2, 3, 4, 5};
int *p = arr;

printf("sizeof(arr) = %zu\n", sizeof(arr));  // 輸出？
printf("sizeof(p) = %zu\n", sizeof(p));      // 輸出？
```
<details>
<summary>點擊查看答案</summary>

```
sizeof(arr) = 20  （5 個 int * 4 bytes = 20）
sizeof(p) = 8     （64位元系統的指標大小）

注意：
- sizeof(arr) 回傳整個陣列的大小
- sizeof(p) 回傳指標變數本身的大小（和系統位元數有關）
```
</details>

---

**Q5：指標與陣列的差異**
```c
char str1[] = "Hello";
char *str2 = "Hello";

str1[0] = 'h';   // 這行會怎樣？
str2[0] = 'h';   // 這行會怎樣？
```
<details>
<summary>點擊查看答案</summary>

```
str1[0] = 'h';  → 合法，可以修改
str2[0] = 'h';  → 未定義行為（可能 Segmentation Fault）

解釋：
- str1 是陣列，在 stack 上分配記憶體，可以修改
- str2 是指標，指向字串常量（通常放在 read-only 區段），不可修改
```
</details>

---

## 🔴 第二類：記憶體管理 — 必考！

**Q6：Stack vs Heap**
```
Stack（堆疊）：
- 自動管理（函式結束自動釋放）
- 速度快
- 大小有限（通常 1-8 MB）
- 區域變數、函式參數

Heap（堆積）：
- 手動管理（malloc/free）
- 速度較慢
- 大小較大（受限於系統記憶體）
- 動態分配的資料
```

**Q7：以下程式碼有什麼問題？**
```c
char* getString() {
    char str[] = "Hello";
    return str;
}

int main() {
    char *s = getString();
    printf("%s\n", s);
    return 0;
}
```
<details>
<summary>點擊查看答案</summary>

```
問題：回傳指向區域變數的指標（Dangling Pointer）

str 是區域變數，存在 stack 上。
函式結束後，str 的記憶體被釋放。
回傳的指標指向已釋放的記憶體 → 未定義行為。

修正方式：
1. 使用 static：static char str[] = "Hello";
2. 使用 malloc：char *str = malloc(6); strcpy(str, "Hello");
3. 回傳字串常量：return "Hello"; （因為字串常量存在 data section）
```
</details>

---

**Q8：Memory Leak 範例**
```c
void func() {
    int *p = (int*)malloc(10 * sizeof(int));
    // 使用 p...
    // 函式結束，但沒有 free(p)
}
```
<details>
<summary>點擊查看答案</summary>

```
問題：Memory Leak（記憶體洩漏）

malloc 分配的記憶體在 heap 上，不會自動釋放。
如果沒有 free()，這塊記憶體會一直被佔用。
長時間運行的程式（如 BMC 韌體）會導致記憶體耗盡。

修正：在不需要時 free(p);
```
</details>

---

## 🔴 第三類：位元運算 — 必考！

**Q9：基本位元運算**
```c
// 常用運算子
& (AND)    |  (OR)     ^ (XOR)
~ (NOT)    << (左移)   >> (右移)

// 範例
unsigned char a = 0b11001010;  // 202
unsigned char b = 0b01100110;  // 102

a & b = ?
a | b = ?
a ^ b = ?
~a = ?
a << 2 = ?
a >> 3 = ?
```
<details>
<summary>點擊查看答案</summary>

```
a & b = 0b01000010 = 66   (兩者都是 1 才是 1)
a | b = 0b11101110 = 238  (任一為 1 就是 1)
a ^ b = 0b10101100 = 172  (不同為 1，相同為 0)
~a    = 0b00110101 = 53   (0 和 1 互換，假設 8-bit)
a << 2 = 0b00101000 = 40  (左移 2 位，等於 *4，但超出的位元遺失)
a >> 3 = 0b00011001 = 25  (右移 3 位，等於 /8)
```
</details>

---

**Q10：SET、CLEAR、TOGGLE 特定 bit**
```c
// 這是嵌入式開發最常用的操作！

// 假設 reg 是 8-bit 暫存器
unsigned char reg = 0b00000000;

// 設定第 3 bit 為 1 (SET)
reg = ?

// 清除第 5 bit 為 0 (CLEAR)
reg = 0b11111111;
reg = ?

// 切換第 2 bit (TOGGLE)
reg = 0b00000100;
reg = ?
```
<details>
<summary>點擊查看答案</summary>

```c
// SET bit n：使用 OR
reg = reg | (1 << 3);
// 或簡寫：reg |= (1 << 3);
// 結果：0b00001000

// CLEAR bit n：使用 AND + NOT
reg = reg & ~(1 << 5);
// 或簡寫：reg &= ~(1 << 5);
// 結果：0b11011111

// TOGGLE bit n：使用 XOR
reg = reg ^ (1 << 2);
// 或簡寫：reg ^= (1 << 2);
// 結果：0b00000000（因為原本是 1，XOR 後變 0）

// 常見巨集定義
#define SET_BIT(reg, n)    ((reg) |= (1 << (n)))
#define CLEAR_BIT(reg, n)  ((reg) &= ~(1 << (n)))
#define TOGGLE_BIT(reg, n) ((reg) ^= (1 << (n)))
#define CHECK_BIT(reg, n)  ((reg) & (1 << (n)))
```
</details>

---

**Q11：判斷數字是否為 2 的冪次**
```c
int isPowerOfTwo(unsigned int n) {
    // 你的答案？
}
```
<details>
<summary>點擊查看答案</summary>

```c
int isPowerOfTwo(unsigned int n) {
    return n != 0 && (n & (n - 1)) == 0;
}

解釋：
- 2 的冪次在二進位中只有一個 bit 是 1
- 例如：8 = 0b1000，8-1 = 7 = 0b0111
- 8 & 7 = 0b1000 & 0b0111 = 0
- 如果 n & (n-1) == 0，代表只有一個 bit 是 1
```
</details>

---

**Q12：計算二進位中 1 的個數**
```c
int countSetBits(unsigned int n) {
    // 你的答案？
}
```
<details>
<summary>點擊查看答案</summary>

```c
// 方法一：逐位檢查
int countSetBits(unsigned int n) {
    int count = 0;
    while (n) {
        count += (n & 1);
        n >>= 1;
    }
    return count;
}

// 方法二：Brian Kernighan's Algorithm（更快）
int countSetBits(unsigned int n) {
    int count = 0;
    while (n) {
        n = n & (n - 1);  // 消除最低位的 1
        count++;
    }
    return count;
}
```
</details>

---

## 🔴 第四類：關鍵字 — 必考！

**Q13：volatile 的用途**
```
volatile 告訴編譯器：這個變數的值可能被外部因素改變，
不要進行優化，每次都要從記憶體讀取。

使用場景：
1. 硬體暫存器
2. 中斷服務程式（ISR）中修改的變數
3. 多執行緒共享的變數

範例：
volatile int *hardware_reg = (volatile int *)0x40000000;
```

**Q14：static 的三種用法**
```c
// 1. 靜態區域變數：值在函式呼叫間保留
void counter() {
    static int count = 0;
    count++;
    printf("%d\n", count);
}
// 多次呼叫會輸出 1, 2, 3, 4...

// 2. 靜態全域變數：限制作用域在該檔案內
static int file_scope_var = 10;
// 其他 .c 檔案無法存取這個變數

// 3. 靜態函式：限制作用域在該檔案內
static void helper_function() { }
// 其他 .c 檔案無法呼叫這個函式
```

**Q15：const 的用法**
```c
const int a = 10;           // a 不可修改
int const b = 20;           // 同上，b 不可修改

const int *p1;              // p1 指向的值不可修改，但 p1 可以指向別的地方
int * const p2;             // p2 不可指向別的地方，但 p2 指向的值可以修改
const int * const p3;       // 都不可修改

// 記憶方法：const 在 * 左邊 → 值不可改
//           const 在 * 右邊 → 指標不可改
```

---

## 🔴 第五類：結構體與聯集 — 常考

**Q16：struct padding**
```c
struct Example1 {
    char a;      // 1 byte
    int b;       // 4 bytes
    char c;      // 1 byte
};

struct Example2 {
    char a;      // 1 byte
    char c;      // 1 byte
    int b;       // 4 bytes
};

printf("sizeof(Example1) = %zu\n", sizeof(struct Example1));  // 輸出？
printf("sizeof(Example2) = %zu\n", sizeof(struct Example2));  // 輸出？
```
<details>
<summary>點擊查看答案</summary>

```
sizeof(Example1) = 12
sizeof(Example2) = 8

解釋（假設 4-byte alignment）：

Example1:
| a |pad|pad|pad| b  b  b  b | c |pad|pad|pad|
  1    3        4             1    3          = 12 bytes

Example2:
| a | c |pad|pad| b  b  b  b |
  1   1   2       4            = 8 bytes

結論：重新排列成員順序可以減少記憶體浪費
```
</details>

---

**Q17：union 的用途**
```c
// union 所有成員共用同一塊記憶體

union Data {
    int i;
    float f;
    char str[20];
};

printf("sizeof(union Data) = %zu\n", sizeof(union Data));  // 輸出？

// 用途：解析硬體暫存器
union Register {
    uint32_t raw;
    struct {
        uint32_t bit0_3  : 4;
        uint32_t bit4_7  : 4;
        uint32_t bit8_15 : 8;
        uint32_t bit16_31: 16;
    } bits;
};
```
<details>
<summary>點擊查看答案</summary>

```
sizeof(union Data) = 20

union 的大小 = 最大成員的大小（str[20] = 20 bytes）

用途：
1. 節省記憶體（當一次只使用一個成員時）
2. 同一塊資料用不同方式解讀（例如暫存器的 raw value 和 bit fields）
```
</details>

---

## 🔴 第六類：預處理器 — 常考

**Q18：#define 陷阱**
```c
#define SQUARE(x) x * x

int a = SQUARE(3);      // 結果？
int b = SQUARE(1 + 2);  // 結果？
```
<details>
<summary>點擊查看答案</summary>

```c
int a = SQUARE(3);      // 展開為 3 * 3 = 9 ✓
int b = SQUARE(1 + 2);  // 展開為 1 + 2 * 1 + 2 = 1 + 2 + 2 = 5 ✗（期望是 9）

正確寫法：
#define SQUARE(x) ((x) * (x))

這樣 SQUARE(1 + 2) 展開為 ((1 + 2) * (1 + 2)) = 9 ✓
```
</details>

---

**Q19：#define vs inline function**
```c
// #define 是文字替換，發生在編譯前
// inline function 是真正的函式，有型別檢查

#define MAX(a, b) ((a) > (b) ? (a) : (b))

inline int max(int a, int b) {
    return a > b ? a : b;
}

// 差異：
int x = 5, y = 3;
int m1 = MAX(x++, y++);  // 問題：x 可能被增加兩次！
int m2 = max(x++, y++);  // 安全：x 只增加一次
```

---

## 🔴 第七類：Linked List — 常考手寫

**Q20：反轉 Linked List**
```c
struct Node {
    int data;
    struct Node *next;
};

struct Node* reverseList(struct Node *head) {
    // 你的答案？
}
```
<details>
<summary>點擊查看答案</summary>

```c
struct Node* reverseList(struct Node *head) {
    struct Node *prev = NULL;
    struct Node *curr = head;
    struct Node *next = NULL;
    
    while (curr != NULL) {
        next = curr->next;  // 先儲存下一個節點
        curr->next = prev;  // 反轉指標方向
        prev = curr;        // prev 前進
        curr = next;        // curr 前進
    }
    
    return prev;  // prev 現在是新的 head
}
```
</details>

---

## ✅ 練習計劃

| 週 | 題目範圍 | 每日練習量 |
|---|---------|-----------|
| 1 | Q1-Q5（指標）| 2-3 題 |
| 2 | Q6-Q12（記憶體、位元運算）| 2-3 題 |
| 3 | Q13-Q19（關鍵字、struct、macro）| 2-3 題 |
| 4 | Q20 + 綜合複習 | 模擬面試 |
